{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product List</h1>
    <button ><a href ="{{path('addbrand')}}">Add Brand</a></button>
    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Name</th>
                <th>Actions</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
         {% for p in brand %}
            <tr>
                <td>{{ p.id }}</td>
                <td>{{ p.namesup }}</td>
                
                <td>
                    <a href="{{ path('edit', { 'id': p.id }) }}">Edit</a>
                    
                </td>
                 <td>
                    <a href="{{ path('delete_brand', { 'id': p.id }) }}"><i class="fa-solid fa-trash-can"></i></a>
                    
                </td>
               
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
